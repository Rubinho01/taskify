<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= quadro.quanome %> - Taskify</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      font-family: sans-serif;
      background-color: #f5f5f5;
      min-height: 100vh;
      margin: 0;
    }
    header {
      background-color: #e61313;
      color: white;
      padding: 1rem 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    #userMenu a:hover {
      background-color: #b91313;
    }
    #userMenu a {
      transition: background-color 0.2s;
    }
    .card-main {
      max-width: 480px;
      margin: 2rem auto;
      background-color: white;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .task-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #fff3f3;
      border-radius: 8px;
      padding: 0.5rem 1rem;
      margin-bottom: 0.7rem;
      font-weight: 500;
      text-decoration: none;
      color: #333;
      transition: background-color 0.3s;
    }
    .task-item:hover {
      background-color: #ffd6d6;
      text-decoration: none;
      color: #b30000;
    }
    .task-checkbox {
      width: 20px;
      height: 20px;
      cursor: default;
    }
    .add-task-btn {
      background-color: #e61313;
      color: white;
      border: none;
      border-radius: 50%;
      width: 38px;
      height: 38px;
      font-size: 28px;
      line-height: 28px;
      text-align: center;
      cursor: pointer;
      transition: background-color 0.3s;
    }
    .add-task-btn:hover {
      background-color: #b30f0f;
      color: white;
      text-decoration: none;
    }
    .header-title {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 1rem;
      flex-wrap: wrap;
    }
    .header-title h2 {
      margin: 0;
      font-weight: 700;
      color: #b30000;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .favorite-board-btn {
      background: none;
      border: none;
      outline: none;
      padding: 0;
      color: gold;
      transition: color 0.2s;
      cursor: pointer;
    }
    .favorite-board-btn:hover i {
      color: orange;
    }
    .btn-sm-custom {
      font-size: 0.8rem;
      padding: 0.25rem 0.5rem;
      border-radius: 0.375rem;
      border: 1.5px solid transparent;
      cursor: pointer;
      transition: background-color 0.3s, border-color 0.3s;
      display: flex;
      align-items: center;
      gap: 0.3rem;
      color: #b30000;
      background-color: transparent;
    }
    .btn-sm-custom:hover {
      background-color: #f8d7da;
      border-color: #b30000;
      color: #7a0000;
      text-decoration: none;
    }
    .btn-sm-danger {
      color: #b30000;
      border-color: #b30000;
    }
    .btn-sm-danger:hover {
      background-color: #f5c6cb;
      color: #7a0000;
    }
  </style>
</head>
<body>

  <%- include('LifeQuality/header') %>

  <main class="card-main">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <a href="/boards" class="btn btn-outline-danger">&larr; Voltar</a>
      <div class="header-title" style="margin: 0;">
        <h2 class="d-flex align-items-center gap-2">
          <%= quadro.quanome %>
          <button class="favorite-board-btn" data-board-id="<%= quadro.quaid %>" title="Favoritar quadro">
            <i class="bi <%= quadro.favorito ? 'bi-star-fill' : 'bi-star' %>" style="font-size: 1.5rem;"></i>
          </button>
        </h2>
        
        <!-- Botões Editar e Excluir -->
        <a href="/board/<%= quadro.quaid %>/edit" class="btn-sm-custom" title="Editar quadro">
          <i class="bi bi-pencil"></i> Editar
        </a>

        <a href="/board/<%= quadro.quaid %>/delete" class="btn-sm-custom btn-sm-danger"
           onclick="return confirm('Tem certeza que deseja excluir este quadro?');"
           title="Excluir quadro">
          <i class="bi bi-trash"></i> Excluir
        </a>

        <a href="/board/<%= quadro.quaid %>/new-task" class="add-task-btn" title="Adicionar nova tarefa">+</a>
      </div>
    </div>

    <% if (tarefas.length === 0) { %>
      <p>No tasks registered.</p>
    <% } else { %>
      <% tarefas.forEach(t => { %>
        <a href="/board/<%= quadro.quaid %>/task/<%= t.tarid %>" class="task-item">
          <span><%= t.tarnome %></span>
          <input type="checkbox" class="task-checkbox" disabled <%= t.tarstatus === 1 ? 'checked' : '' %> />
        </a>
      <% }) %>
    <% } %>
  </main>

  <script>
    const userIcon = document.getElementById('userIcon');
    const userMenu = document.getElementById('userMenu');
    userIcon?.addEventListener('click', () => {
      userMenu.style.display = (userMenu.style.display === 'block') ? 'none' : 'block';
    });
    document.addEventListener('click', (event) => {
      if (!userIcon?.contains(event.target) && !userMenu?.contains(event.target)) {
        userMenu.style.display = 'none';
      }
    });
    document.querySelector('.favorite-board-btn')?.addEventListener('click', async function (e) {
      e.preventDefault();
      const icon = this.querySelector('i');
      const boardId = this.getAttribute('data-board-id');
      const isFavorited = icon.classList.contains('bi-star-fill');

      await fetch(`/board/${boardId}/favorite`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ favorito: !isFavorited })
      });

      icon.classList.toggle('bi-star');
      icon.classList.toggle('bi-star-fill');
    });
  </script>
</body>
</html>
