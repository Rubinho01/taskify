<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Quadros</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(to bottom right, #fef6f6, #ffe8e8);
      display: flex;
      min-height: 100vh;
      overflow-x: hidden;
    }

    aside {
      width: 240px;
      background-color: #fff6f6;
      border-right: 1px solid #f1cfcf;
      padding: 20px;
      display: flex;
      flex-direction: column;
      gap: 24px;
      box-shadow: 2px 0 5px rgba(0,0,0,0.05);
    }

    aside h2 {
      font-size: 18px;
      color: #e61313;
      margin-bottom: 10px;
    }

    aside .menu, aside .boards {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    aside .menu button, aside .boards div {
      padding: 10px;
      border-radius: 8px;
      cursor: pointer;
      color: #333;
      transition: background 0.2s;
      border: none;
      background: none;
      text-align: left;
    }

    aside .menu button:hover, aside .boards div:hover {
      background-color: #ffeaea;
    }

    .active {
      background-color: #e61313;
      color: white;
      font-weight: bold;
    }

    .main {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    header {
      background-color: #e61313;
      color: white;
      padding: 16px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .board-container {
      display: flex;
      padding: 24px;
      gap: 20px;
      overflow-x: auto;
      flex-wrap: nowrap;
    }

    .column {
      background-color: #fff6f6;
      border-radius: 18px;
      padding: 16px;
      width: 260px;
      flex-shrink: 0;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
      display: flex;
      flex-direction: column;
    }

    .column h3 {
      font-size: 16px;
      margin-bottom: 12px;
      color: #222;
    }

    .task-card {
      background-color: white;
      border-radius: 14px;
      padding: 12px;
      margin-bottom: 12px;
      box-shadow: 0 1px 4px rgba(0, 0, 0, 0.08);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .card-content {
      display: flex;
      gap: 12px;
      align-items: center;
    }

    .card-content strong {
      font-weight: 600;
      font-size: 14px;
    }

    .card-content small {
      font-size: 12px;
      color: #555;
    }

    /* Botão discreto para adicionar quadro */
    .add-board-btn {
      background-color: transparent;
      border: 1px solid #e61313;
      color: #e61313;
      border-radius: 8px;
      padding: 8px 12px;
      cursor: pointer;
      height: fit-content;
      flex-shrink: 0;
      transition: background-color 0.3s ease, color 0.3s ease;
      font-weight: 600;
      font-size: 14px;
      align-self: center;
    }
    .add-board-btn:hover {
      background-color: #e61313;
      color: white;
    }

    /* Estilos para o layout do quadro e tarefas fixas */
    .main-content {
      background-color: #fff6f6;
      border-radius: 18px;
      padding: 20px;
      width: 260px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.05);
      display: flex;
      flex-direction: column;
    }

    .main-content .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 16px;
    }

    .main-content h2 {
      font-size: 18px;
      color: #222;
    }

    .main-content button {
      background-color: #e61313;
      border: none;
      color: white;
      border-radius: 50%;
      width: 28px;
      height: 28px;
      font-size: 20px;
      line-height: 1;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .main-content button:hover {
      background-color: #b30f0f;
    }

    .task {
      display: flex;
      align-items: center;
      margin-bottom: 12px;
    }

    .task label {
      flex: 1;
      font-weight: 600;
      color: #555;
      cursor: pointer;
    }

    .task input[type="checkbox"] {
      width: 18px;
      height: 18px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <aside>
    <h2>Logo TaskyFy</h2>
    <div class="menu">
      <% quadrosUsuario.forEach(q => { %>
        <button onclick="location.href='/board/<%= q.quaid %>'"><%= q.quanome %></button>
      <% }) %>
    </div>

    <div>
      <h2>Quadros Recentes</h2>
      <div class="boards">
        <div class="active"><%= quadro.quanome %></div>
      </div>
    </div>
  </aside>

  <div class="main">
    <header>
      <strong>Boards</strong>
      <input type="text" placeholder="Pesquisar..." style="padding: 6px 10px; border-radius: 6px; border: none;">
    </header>

    <div class="board-container">
      <% if (quadrosUsuario && quadrosUsuario.length > 0) { %>
                        <% quadrosUsuario.forEach(function(quadro) { %>
                          <a href="/board/<%= quadro.quaid %>" style="text-decoration: none;">
                            <div class="main-content" style="background-color: #fff6f6; border-radius: 18px; padding: 20px; width: 260px; box-shadow: 0 4px 8px rgba(0,0,0,0.05); display: flex; flex-direction: column; transition: transform 0.2s ease;">
                              <div class="header" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px;">
                                <h2 style="font-size: 18px; color: #222;"><%= quadro.quanome %></h2>
                                <button onclick="event.preventDefault(); window.location.href='/board/<%= quadro.quaid %>/new-task';" style="background-color: #e61313; border: none; color: white; border-radius: 50%; width: 28px; height: 28px; font-size: 20px; line-height: 1; cursor: pointer;">+</button>
                              </div>

                              <% tarefas.forEach(t => { %>
                                <div class="bg-white rounded-3 p-2 mb-2 d-flex justify-content-between align-items-center">
                                  <a href="/board/<%= quadro.quaid %>/task/<%= t.tarid %>" class="fw-medium" for="tarefa1" style="color: inherit; text-decoration: none;"><%= t.tarnome %></a>
                                  <input type="checkbox" id="tarefa1" class="form-check-input" <%= t.tarstatus === 1 ? 'checked disabled' : 'disabled' %>>
                                </div>
                              <% }); %>
                            </div>
                          </a>
                        <% }) %>
                <% } else { %>
                  <p style="padding: 24px;">Nenhum quadro encontrado.</p>
                <% } %>
      <button class="add-board-btn" onclick="location.href='/boardForm'">+ Adicionar Quadro</button>
    </div>
  </div>
</body>
</html>
