<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Visualizar Perfil</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(to bottom right, #f1cfcf, #f1cfcf);
      display: flex;
      min-height: 100vh;
      margin: 0;
      overflow-x: visible;
    }

    aside {
      width: 240px;
      background-color: #f1cfcf;
      border-right: 1px solid #f1cfcf;
      padding: 20px;
      display: flex;
      flex-direction: column;
      gap: 24px;
      box-shadow: 2px 0 5px rgba(0,0,0,0.05);
      flex-shrink: 0;
      height: 100vh;
      position: sticky;
      top: 0;
    }

    .main {
      flex: 1;
      display: flex;
      flex-direction: column;
      overflow-x: visible;
    }

    .profile-container {
      max-width: 5000px;
      width: 720px;
      margin: 9rem auto;
      padding: 0 1rem;
    }

    .profile-container h2 {
      color: #e61313;
      font-weight: 600;
      border-bottom: 1px solid #e61313;
      padding-bottom: 6px;
      margin-bottom: 24px;
    }

    .bio-box {
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .bio-box h4 {
      font-weight: 600;
      color: #e61313;
    }

    @media (max-width: 1024px) {
      .column { width: 220px; }
      aside { width: 200px; }
    }

    @media (max-width: 768px) {
      aside { display: none; }
      .main { flex: 1 1 100%; }
    }
  </style>
</head>
<body>

  <%- include('LifeQuality/sidebar') %>

  <div class="main">
    <%- include('LifeQuality/header') %>

    <div class="profile-container">
      <h2>Seu Perfil</h2>

      <div class="bio-box">
        <h4><i class="bi bi-person-circle me-2"></i><%= usuario.usunome %></h4>
        <hr />
        <p class="mt-3">
          <% if (usuario.usubio && usuario.usubio.trim()) { %>
            <%= usuario.usubio %>
          <% } else { %>
            <em>Este usuário ainda não escreveu uma biografia.</em>
          <% } %>
        </p>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
