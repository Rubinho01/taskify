<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card-custom {
      border-radius: 1rem;
      box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
    }
    .btn-soft {
      background-color: #f8f9fa;
      border: none;
    }
    .btn-soft:hover {
      background-color: #e2e6ea;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
    <div class="container">
      <span class="navbar-brand fw-bold text-danger">
        <i class="bi bi-speedometer2"></i> Taskify Admin Dashboard
      </span>
      <div class="ms-auto d-flex align-items-center">
        <span class="me-3 fw-semibold">Olá, <%= nome %></span>
        <a href="/admin/sair" class="btn btn-outline-danger btn-sm">
          <i class="bi bi-box-arrow-right"></i> Sair
        </a>
      </div>
    </div>
  </nav>
  <div class="container py-5">
    <h2 class="text-center text-danger fw-bold mb-5"><u>Dashboard</u></h2>

    <div class="row justify-content-center g-5">
      <div class="col-md-5">
        <div class="card card-custom p-4 bg-white text-center">
          <h5 class="text-danger fw-bold mb-0"><i class="bi bi-bar-chart-line"></i> Graph</h5>
          <p class="text-muted">Estatísticas Totais</p>
            <div class="d-flex justify-content-between fs-5">
            <span>Admins:</span> <span><strong><%= totalAdmins %></strong></span>
          </div>
          <div class="d-flex justify-content-between fs-5">
            <span>Tasks:</span> <span><strong><%= totalTasks %></strong></span>
          </div>
          <div class="d-flex justify-content-between fs-5">
            <span>Boards:</span> <span><strong><%= totalBoards %></strong></span>
          </div>
          <div class="d-flex justify-content-between fs-5">
            <span>Users:</span> <span><strong><%= totalUsers%></strong></span>
          </div>
        </div>
      </div>
      <div class="col-md-5">
        <div class="card card-custom p-4 bg-white text-center">
          <h5 class="text-danger fw-bold mb-0"><i class="bi bi-person-gear"></i> Main</h5>
          <p class="text-muted">Gerenciamento</p>
          <div class="d-grid gap-3">
            <button class="btn btn-soft fs-5">Admins</button>
            <button class="btn btn-soft fs-5">Boards</button>
            <button class="btn btn-soft fs-5">Users</button>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
